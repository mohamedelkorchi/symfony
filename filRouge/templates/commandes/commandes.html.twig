{% extends 'base.html.twig' %}

{% block title %}Mes commandes{% endblock %}

{% block body %}

<h1></h1>
<h2></h2>


<div class="container">
    <table class="table table-striped table-dark ">
        <tr>
        <th>id</th>
        <th>Date commande</th>
        <th></th>
    
        </tr>
        {% for commande in commandes %}
            <tr>
            <td>{{commande.id}}</td>
            <td>{{commande.dateCommande | date("d/m/Y")}}</td>
            <td><a class="btn btn-primary mb-5 vider" href="/commandes_details/{{commande.id}}"> ...</a></td>
            </tr>
        {% endfor %}
    </table>
</div>



{% endblock %}